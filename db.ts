export function storeInDB(event: any) {
    // we have to handle by ourselves
    console.log("storeInDB", event)
}

export function saveSnapshotInDB(snapshot: any) {
    // we have to handle by ourselves
    console.log("saveSnapshotInDB", snapshot)
}
